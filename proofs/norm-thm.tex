\jtheorem{Proof of \autoref{thm:norm-thm}}{
  \jgivengoalThree{
    \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \tau : K$  
  }{
   $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : K$  
  }{
   $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : K$
  }{
   $\texttt{eval}(\tau) \; \texttt{nf}$  
  }
  
  \caseText{By induction on the derivation of $\Psi ; \Theta ; \Delta \vdash \tau : K$}  
  
  \jcase{1}{K-Var}{
    \caseText{Immediate.}  
  }
  
  \jcase{2}{K-Unit}{
    \caseText{Immediate.}  
  }
  
  \jcase{3}{K-Arr}{
   \jgivengoalThree{
     \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \loli \tau_2 : \star$
     
     \caseFact{2} $\Psi ; \Theta ; \Delta \pvdash \tau_1 : \star$
     
     \caseFact{3} $\Psi ; \Theta ; \Delta \pvdash \tau_2 : \star$
   }{
    $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) \loli \texttt{eval}(\tau_1) : \star$   
   }{
     $\Psi ; \Theta ; \Delta \pvdash \tau_1 \loli \tau_2 \equiv \texttt{eval}(\tau_1) \loli \texttt{eval}(\tau_1) : \star$   
   }{
     $\texttt{eval}(\tau_1) \loli \texttt{eval}(\tau_1) \; \texttt{nf}$
   }
   
   \caseText{By IH on (2)}
   
   \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) : \star$
   
   \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \equiv \texttt{eval}(\tau_1) : \star$
   
   \caseFact{6} $\texttt{eval}(\tau_1) \; \texttt{nf}$
   
   \caseText{By IH on (3)}
   
   \caseFact{7} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_2) : \star$
   
   \caseFact{8} $\Psi ; \Theta ; \Delta \pvdash \tau_2 \equiv \texttt{eval}(\tau_2) : \star$
   
   \caseFact{9} $\texttt{eval}(\tau_2) \; \texttt{nf}$
   
   \caseText{Goal 1 follows by K-Arr on (4) and (7)}
   
   \caseText{Goal 2 follows by S-Arr twice on (5) and (8)}
   
   \caseText{Goal 3 follows from (6) and (9)}
  }
  
  \jcase{4}{K-Tensor}{
   \jgivengoalThree{
     \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \otimes \tau_2 : \star$
     
     \caseFact{2} $\Psi ; \Theta ; \Delta \pvdash \tau_1 : \star$
     
     \caseFact{3} $\Psi ; \Theta ; \Delta \pvdash \tau_2 : \star$
   }{
    $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) \otimes \texttt{eval}(\tau_1) : \star$   
   }{
     $\Psi ; \Theta ; \Delta \pvdash \tau_1 \otimes \tau_2 \equiv \texttt{eval}(\tau_1) \otimes \texttt{eval}(\tau_1) : \star$   
   }{
     $\texttt{eval}(\tau_1) \otimes \texttt{eval}(\tau_1) \; \texttt{nf}$
   }
   
   \caseText{By IH on (2)}
   
   \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) : \star$
   
   \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \equiv \texttt{eval}(\tau_1) : \star$
   
   \caseFact{6} $\texttt{eval}(\tau_1) \; \texttt{nf}$
   
   \caseText{By IH on (3)}
   
   \caseFact{7} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_2) : \star$
   
   \caseFact{8} $\Psi ; \Theta ; \Delta \pvdash \tau_2 \equiv \texttt{eval}(\tau_2) : \star$
   
   \caseFact{9} $\texttt{eval}(\tau_2) \; \texttt{nf}$
   
   \caseText{Goal 1 follows by K-Tensor on (4) and (7)}
   
   \caseText{Goal 2 follows by S-Tensor twice on (5) and (8)}
   
   \caseText{Goal 3 follows from (6) and (9)}
  }
  
  \jcase{5}{K-With}{
   \jgivengoalThree{
     \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \amp \tau_2 : \star$
     
     \caseFact{2} $\Psi ; \Theta ; \Delta \pvdash \tau_1 : \star$
     
     \caseFact{3} $\Psi ; \Theta ; \Delta \pvdash \tau_2 : \star$
   }{
    $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) \amp \texttt{eval}(\tau_1) : \star$   
   }{
     $\Psi ; \Theta ; \Delta \pvdash \tau_1 \amp \tau_2 \equiv \texttt{eval}(\tau_1) \amp \texttt{eval}(\tau_1) : \star$   
   }{
     $\texttt{eval}(\tau_1) \amp \texttt{eval}(\tau_1) \; \texttt{nf}$
   }
   
   \caseText{By IH on (2)}
   
   \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) : \star$
   
   \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \equiv \texttt{eval}(\tau_1) : \star$
   
   \caseFact{6} $\texttt{eval}(\tau_1) \; \texttt{nf}$
   
   \caseText{By IH on (3)}
   
   \caseFact{7} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_2) : \star$
   
   \caseFact{8} $\Psi ; \Theta ; \Delta \pvdash \tau_2 \equiv \texttt{eval}(\tau_2) : \star$
   
   \caseFact{9} $\texttt{eval}(\tau_2) \; \texttt{nf}$
   
   \caseText{Goal 1 follows by K-With on (4) and (7)}
   
   \caseText{Goal 2 follows by S-With twice on (5) and (8)}
   
   \caseText{Goal 3 follows from (6) and (9)}
  }
  
  \jcase{6}{K-Sum}{
   \jgivengoalThree{
     \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \oplus \tau_2 : \star$
     
     \caseFact{2} $\Psi ; \Theta ; \Delta \pvdash \tau_1 : \star$
     
     \caseFact{3} $\Psi ; \Theta ; \Delta \pvdash \tau_2 : \star$
   }{
    $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) \oplus \texttt{eval}(\tau_1) : \star$   
   }{
     $\Psi ; \Theta ; \Delta \pvdash \tau_1 \oplus \tau_2 \equiv \texttt{eval}(\tau_1) \oplus \texttt{eval}(\tau_1) : \star$   
   }{
     $\texttt{eval}(\tau_1) \oplus \texttt{eval}(\tau_1) \; \texttt{nf}$
   }
   
   \caseText{By IH on (2)}
   
   \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_1) : \star$
   
   \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau_1 \equiv \texttt{eval}(\tau_1) : \star$
   
   \caseFact{6} $\texttt{eval}(\tau_1) \; \texttt{nf}$
   
   \caseText{By IH on (3)}
   
   \caseFact{7} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau_2) : \star$
   
   \caseFact{8} $\Psi ; \Theta ; \Delta \pvdash \tau_2 \equiv \texttt{eval}(\tau_2) : \star$
   
   \caseFact{9} $\texttt{eval}(\tau_2) \; \texttt{nf}$
   
   \caseText{Goal 1 follows by K-Sum on (4) and (7)}
   
   \caseText{Goal 2 follows by S-Sum twice on (5) and (8)}
   
   \caseText{Goal 3 follows from (6) and (9)}
  }
  
  \jcase{7}{K-Bang}{
   \jgivengoalThree{
     \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash !\tau : \star$
     
     \caseFact{2} $\Psi ; \Theta ; \Delta \pvdash \tau \star$
   }{
    $\Psi ; \Theta ; \Delta \pvdash !\texttt{eval}(\tau) : \star$   
   }{
     $\Psi ; \Theta ; \Delta \pvdash !\tau \equiv !\texttt{eval}(\tau) :  \star$   
   }{
     $!\texttt{eval}(\tau) \; \texttt{nf}$
   }
   
   \caseText{By IH on (2)}
   
   \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
   
   \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
   
   \caseFact{6} $\texttt{eval}(\tau) \; \texttt{nf}$
   
   \caseText{Goal 1 follows by K-Bang on (4)}
   
   \caseText{Goal 2 follows by S-Sum on (5)}
   
   \caseText{Goal 3 follows from (6)}
  }
  
  \jcase{8}{K-IForall}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \forall i : S.\tau : \star$
      
      \caseFact{2} $\Psi ; \Theta, i : S ; \Delta \pvdash \tau : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \forall i : S. \texttt{eval}(\tau) : \star$
    }{
       $\Psi ; \Theta ; \Delta \pvdash \forall i : S. \tau \equiv \forall i : S. \texttt{eval}(\tau) : \star$
    }{
      $\forall i : S. \texttt{eval}(\tau) \; \texttt{nf}$
    }
    \caseText{By IH on (2)}
    
    \caseFact{3} $\Psi ; \Theta, i : S ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{4} $\Psi ; \Theta, i : S ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{5} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-IForall on (3)}
    
    \caseText{Goal 2 follows by S-IForall on (4)}
    
    \caseText{Goal 3 is immediate from (5)}
  }
  
  \jcase{9}{K-IExists}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \exists i : S.\tau : \star$
      
      \caseFact{2} $\Psi ; \Theta, i : S ; \Delta \pvdash \tau : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \exists i : S. \texttt{eval}(\tau) : \star$
    }{
       $\Psi ; \Theta ; \Delta \pvdash \exists i : S. \tau \equiv \exists i : S. \texttt{eval}(\tau) : \star$
    }{
      $\exists i : S. \texttt{eval}(\tau) \; \texttt{nf}$
    }
    \caseText{By IH on (2)}
    
    \caseFact{3} $\Psi ; \Theta, i : S ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{4} $\Psi ; \Theta, i : S ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{5} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-IExists on (3)}
    
    \caseText{Goal 2 follows by S-IExists on (4)}
    
    \caseText{Goal 3 is immediate from (5)}
  }
  \jcase{10}{K-TForall}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \forall \alpha : K.\tau : \star$
      
      \caseFact{2} $\Psi, \alpha : K ; \Theta ; \Delta \pvdash \tau : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \forall \alpha : K. \texttt{eval}(\tau) : \star$
    }{
       $\Psi ; \Theta ; \Delta \pvdash \forall \alpha : K. \tau \equiv \forall \alpha : K. \texttt{eval}(\tau) : \star$
    }{
      $\forall \alpha : K. \texttt{eval}(\tau) \; \texttt{nf}$
    }
    \caseText{By IH on (2)}
    
    \caseFact{3} $\Psi, \alpha : K ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{4} $\Psi, \alpha : K ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{5} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-TForall on (3)}
    
    \caseText{Goal 2 follows by S-TForall on (4)}
    
    \caseText{Goal 3 is immediate from (5)}
  }
  
  \jcase{11}{K-List}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash L^I\tau : \star$
      
      \caseFact{2} $\Psi; \Theta ; \Delta \pvdash \tau : \star$
      
      \caseFact{3} $\Theta ; \Delta \pvdash I : \N$
    }{
      $\Psi ; \Theta ; \Delta \pvdash L^I \left(\texttt{eval}(\tau)\right) : \star$
    }{
       $\Psi ; \Theta ; \Delta \pvdash L^I \tau \equiv L^I \left(\texttt{eval}(\tau)\right) : \star$
    }{
      $L^I \left(\texttt{eval}(\tau)\right) \; \texttt{nf}$
    }
    \caseText{By IH on (2)}
    
    \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{6} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-List on (4) and (3)}
    
    \caseText{Goal 2 follows by S-List on (5), and the fact that $\Theta ; \Delta \vDash I = I$}
    
    \caseText{Goal 3 is immediate from (6)}
  }
  
  \jcase{11}{K-Conj}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \Phi \amp \tau : \star$
      
      \caseFact{2} $\Psi; \Theta ; \Delta \pvdash \tau : \star$
      
      \caseFact{3} $\Theta ; \Delta \pvdash \Phi \, \texttt{wf}$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \Phi \amp \texttt{eval}(\tau) : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \Phi \amp \tau \equiv \Phi \amp \texttt{eval}(\tau) : \star$ 
    }{
      $\Phi \amp \texttt{eval}(\tau) \; \texttt{nf}$
    }
    
    \caseText{By IH on (2)}
    
    \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{6} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-Conj from (4) and (3)}
    
    \caseText{Goal 2 follows by S-Conj from (5) and the fact that $\Theta ; \Delta \vDash \Phi \to \Phi$}
    
    \caseText{Goal 3 follows from (6)}
  }
  
  \jcase{12}{K-Impl}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \Phi \implies \tau : \star$
      
      \caseFact{2} $\Psi; \Theta ; \Delta \pvdash \tau : \star$
      
      \caseFact{3} $\Theta ; \Delta \pvdash \Phi \, \texttt{wf}$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \Phi \implies \texttt{eval}(\tau) : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \Phi \implies \tau \equiv \Phi \implies \texttt{eval}(\tau) : \star$ 
    }{
      $\Phi \amp \texttt{eval}(\tau) \; \texttt{nf}$
    }
    
    \caseText{By IH on (2)}
    
    \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{6} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-Impl from (4) and (3)}
    
    \caseText{Goal 2 follows by S-Impl from (5) and the fact that $\Theta ; \Delta \vDash \Phi \to \Phi$}
    
    \caseText{Goal 3 follows from (6)}
  }
  
  \jcase{13}{K-Monad}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \M \, (I,\vec{p}) \,\tau : \star$
      
      \caseFact{2} $\Psi; \Theta ; \Delta \pvdash \tau : \star$
      
      \caseFact{3} $\Theta ; \Delta \pvdash I : \N$
      
      \caseFact{4} $\Theta ; \Delta \pvdash \vec{p} : \potvec$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \M \, (I,\vec{p}) \, \texttt{eval}(\tau) : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \M \, (I,\vec{p}) \,\tau \equiv \M \, (I,\vec{p}) \, \texttt{eval}(\tau) : \star$ 
    }{
      $\M \, (I,\vec{p}) \, \texttt{eval}(\tau)\; \texttt{nf}$
    }
    
    \caseText{By IH on (2)}
    
    \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{6} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{7} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-Monad on (5), (3), (5)}
    
    \caseText{Goal 2 follows by S-Monad on (6) and the fact that $\Theta ; \Delta \vDash I = I \wedge \vec{p} \leq \vec{p}$}
    
    \caseText{Goal 3 follows from (7)}
  }
  
  \jcase{14}{K-Pot}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash [I|\vec{p}] \,\tau : \star$
      
      \caseFact{2} $\Psi; \Theta ; \Delta \pvdash \tau : \star$
      
      \caseFact{3} $\Theta ; \Delta \pvdash I : \N$
      
      \caseFact{4} $\Theta ; \Delta \pvdash \vec{p} : \potvec$
    }{
      $\Psi ; \Theta ; \Delta \pvdash [I|\vec{p}] \, \texttt{eval}(\tau) : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash [I|\vec{p}] \,\tau \equiv [I|\vec{p}] \, \texttt{eval}(\tau) : \star$ 
    }{
      $[I|\vec{p}] \, \texttt{eval}(\tau)\; \texttt{nf}$
    }
    
    \caseText{By IH on (2)}
    
    \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{6} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{7} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-Pot on (5), (3), (4)}
    
    \caseText{Goal 2 follows by S-Pot on (6) and the fact that $\Theta ; \Delta \vDash I = I \wedge \vec{p} \leq \vec{p}$}
    
    \caseText{Goal 3 follows from (7)}
  }
  
  \jcase{15}{K-ConstPot}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash [I] \,\tau : \star$
      
      \caseFact{2} $\Psi; \Theta ; \Delta \pvdash \tau : \star$
      
      \caseFact{3} $\Theta ; \Delta \pvdash I : \R$
     
    }{
      $\Psi ; \Theta ; \Delta \pvdash [I] \, \texttt{eval}(\tau) : \star$
    }{
      $\Psi ; \Theta ; \Delta \pvdash [I] \,\tau \equiv [I] \, \texttt{eval}(\tau) : \star$ 
    }{
      $[I] \, \texttt{eval}(\tau)\; \texttt{nf}$
    }
    
    \caseText{By IH on (2)}
    
    \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : \star$
    
    \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : \star$
    
    \caseFact{6} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows by K-ConstPot on (4), (3)}
    
    \caseText{Goal 2 follows by S-Pot on (5) and the fact that $\Theta ; \Delta \vDash I = I \wedge \vec{p} \leq \vec{p}$}
    
    \caseText{Goal 3 follows from (6)}
  }
  
  \jcase{15}{K-FamLam}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \lambda i : S. \tau : S \to K$
      
      \caseFact{2} $\Psi; \Theta, i : S ; \Delta \pvdash \tau : K$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \lambda i : S. \texttt{eval}(\tau) : S \to K$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \lambda i : S. \tau \equiv \lambda i : S. \texttt{eval}(\tau) : S \to K$ 
    }{
      $\lambda i : S. \texttt{eval}(\tau)\; \texttt{nf}$
    }
    
    \caseText{By IH on (2)}
    
    \caseFact{3} $\Psi ; \Theta, i : S ; \Delta \pvdash \texttt{eval}(\tau) : K$
    
    \caseFact{4} $\Psi ; \Theta, i : S ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : K$
    
    \caseFact{5} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{Goal 1 follows from K-FamLam on (3)}
    
    \caseText{Goal 2 follows from S-FamLam on (4)}
    
    \caseText{Goal 3 follows from (5)}

  }
  
  \jcase{15}{K-FamApp}{
    \jgivengoalThree{
      \caseFact{1} $\Psi ; \Theta ; \Delta \pvdash \tau \; I : K$
      
      \caseFact{2} $\Psi; \Theta ; \Delta \pvdash \tau : S \to K$
      
      \caseFact{3} $\Theta ; \Delta \pvdash I : S$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau \; I) : K$
    }{
      $\Psi ; \Theta ; \Delta \pvdash \tau \; I \equiv \texttt{eval}(\tau \; I) : K$ 
    }{
      $\texttt{eval}(\tau \; I) \; \texttt{nf}$
    }
    
    \caseText{By IH on (2)}
    
    \caseFact{4} $\Psi ; \Theta ; \Delta \pvdash \texttt{eval}(\tau) : S \to K$
    
    \caseFact{5} $\Psi ; \Theta ; \Delta \pvdash \tau \equiv \texttt{eval}(\tau) : S \to K$
    
    \caseFact{6} $\texttt{eval}(\tau) \; \texttt{nf}$
    
    \caseText{By \autoref{thm:canon-forms} on (4), there are two possibilities for $\texttt{eval}(\tau)$.}
    
    \jsubcase{1}{$\texttt{eval}(\tau)\, \texttt{ne}$}{
      \caseText{Here, $\texttt{eval}(\tau \; I) = \texttt{eval}(\tau) \; I$}
      
      \caseText{Goal 1 follows from T-FamApp on (4) and (3)}
      
      \caseText{Goal 2 follows from S-FamApp on (5) and (3) with $\Theta ; \Theta \vDash I = I$}
      
      \caseText{Goal 3 follows from the fact that $\texttt{eval}(\tau)\, \texttt{ne}$}
    }
    
    \jsubcase{2}{$\texttt{eval}(\tau) = \lambda i : S. \tau'$ and $\tau' \; \texttt{nf}$}{
      \caseText{In this case, $\texttt{eval}(\tau \; I) = \tau'[I/i]$}
      
      \caseText{From \autoref{thm:canon-forms}}
      
      \caseFact{8} $\tau' \; \texttt{nf}$
      
      \caseText{We also know from (4) that}
      
      \caseFact{9} $\Psi ; \Theta ; \Delta \pvdash \lambda i : S. \tau' : S \to K$
      
      \caseText{Inverting (9)}
      
      \caseFact{10} $\Psi ; \Theta, i : S ; \Delta \pvdash \tau' : K$
      
      \caseText{Goal 1 follows from \autoref{thm:idx-idx-subst} on (10) and (3)}
      
      \caseText{By S-FamApp on (5)}
      
      \caseFact{11} $\Psi ; \Theta ; \Delta \pvdash \tau \; I \equiv (\lambda i : S. \tau') \; I : K$
      
      \caseText{By S-FamBeta\{1,2\}}
      
      \caseFact{12} $\Psi ; \Theta ; \Delta \pvdash  (\lambda i : S. \tau') \; I \equiv \tau'[I/i] : K$
      
      \caseText{Goal 2 follows from applying S-Trans to (11) and (12)}
      
      \caseText{Goal 3 follows from applying \autoref{thm:idx-subst-nf} to (8)}
    }

  }
  
  
  
  
}

\iffalse
\begin{proof}
%By induction on $\# \tau$. The base cases (base type and type variables) are immediate. For the inductive case, we break into cases on the syntax of $\tau$, inverting $\Psi ;\Theta ; \Delta \vdash \tau : K$-- since this judgment is syntax directed, we may write this as if it were cases over the derivation.

By induction on $\Psi ; \Theta ; \Delta \vdash \tau : K$.

\begin{enumerate}

  Otherwise, suppose that $\texttt{eval}(\tau) = \lambda i : S. \tau'$ with $\tau' \; \texttt{nf}$ and $\Psi ; \Theta , i : S ; \Delta \vdash \tau' : K$. In this case, $\texttt{eval}(\tau \; I) = \tau'[I/i]$:
  \begin{enumerate}[1.]
   \item By \textbf{Substitution} with $\Theta ; \Delta \vdash I : S$, we have that $\Psi ; \Theta ; \Delta \vdash \tau'[I/i] : K$.
   \item We already know that $\Psi ; \Theta ; \Delta \vdash \tau \equiv \texttt{eval}(\tau) : S \to K$, but $\texttt{eval}(\tau) = \lambda i : S. \tau'$, and so
   $\Psi ; \Theta ; \Delta \vdash \tau \equiv \lambda i : S.\tau' : S \to K$. By S-FamApp, $\Psi ; \Theta ; \Delta \vdash \tau \; I \equiv (\lambda i : S.\tau') \; I : K$. Postcomposing with both directions of S-Fam-Beta-\{1,2\}, we have that $\Psi ; \Theta ; \Delta \vdash \tau \; I \equiv \tau'[I/i] : K$, as required.
   \item Since $\tau'\; \texttt{nf}$, we have by \autoref{thm:idx-subst-nf} that $\tau'[I/i] \; \texttt{nf}$.
  \end{enumerate}
  
\end{enumerate}
\end{proof}

\fi